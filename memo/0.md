OpenJTalkはインストールしているのだが、毎回依存関係をパス解決するのがウザすぎる。そこで簡単に呼び出せるスクリプトを書きたい。以下のような機能を持っていることとする。

* `ojt.sh`というスクリプトファイルで呼び出しを簡略化したい
* 所定のディレクトリにある.htsvoiceファイルの拡張子を除いた名前で使用ボイスを指定したい
	* 存在しない名前ならエラーハンドリングする
		* `指定された音声モデルが存在しません。'ojt.sh list'で使用可能な音声モデルを確認してください。: ${指定された音声モデル文字列}`）
	* デフォルト値を設定したい
		* 音声モデルが存在するルートディレクトリ
		* 音声モデルが存在するルートディレクトリの中のどれか一つをデフォルト使用用にする
* 所定の辞書を設定したい
	* デフォルトパスを設定する（`/usr/local/dic`）
	* 任意パスを引数でセットできるようにしたい
* 入力
	* 文字列
	* テキストファイル
* 出力
	* リアルタイム発話（`-w /dev/stdout`+` | aplay > /dev/null 2>&1`）
	* ファイル出力のみ
		* wav形式（デフォルト）
		* mp3形式
		* ogg形式
		* flac形式
		* 複数（wav,mp3,ogg,flac）
	* リアルタイム発話＆ファイル出力
		* 発話＋複数（wav,mp3,ogg,flac）
* 声質
	* 声の高さを半音単位で変える(`-fm`)
	* 音量（`-g`）

```sh
OJT_VOICE_ROOT=/home/pi/root/sys/env/tool/openjtalk/voice/
OJT_VOICE_EXT=.htsvoice
echo '発話したいテキスト' | open_jtalk \
	-x /usr/local/dic \
	-m "${OJT_VOICE_ROOT}mei_normal${OJT_VOICE_EXT}" \
	-ow /dev/stdout \
	| aplay > /dev/null 2>&1
```

どういうAPIにするか。

```sh
$ ojt.sh --help
OpenJTalk糖衣構文コマンド v1.0.0
[使用例]
echo '発話させたいテキスト' | ojt.sh play -m mei_happy
echo '発話させたいテキスト' | ojt.sh save -m mei_happy -f mp3 -o /tmp/work/0.wav
ojt.sh save -m mei_happy -f mp3 -o /tmp/work/0.wav input.txt
[サブコマンド]
help                       このヘルプを表示する
set voice root {ROOT}      .htsvoiceがインストールされているルートディレクトリ絶対パス
set voice default {DEF}    {ROOT}直下にある.htsvoiceの拡張子を除いたファイル名
talk             発話する
file {format}    音声ファイル出力する（format=wav,mp3,ogg,flac。複数ならカンマ区切り）
tale {format}    発話＋ファイル出力する
[任意引数]
-d --dict     辞書を変更する
-m --model    音声モデルを変更する
-k --key      音程を変更する（1.0=半音↑, -2.0=2半音↓等）
-v --volume   音量を変更する（dB。正数↑, 負数↓）
-f --format   出力形式を変更する（wav,mp3,ogg,flac。複数ならカンマ区切り）
-o --output   出力先を変更する（デフォルトはカレントディレクトリ/ojt.wav）
[位置引数]
0             入力元テキストファイルパス（省略時は標準入力から読み込む）
[初期値]
音声モデル
  Root: /home/pi/root/sys/env/tool/openjtalk/voice/
  Default: mei_normal
辞書:
  Default: /usr/local/dic
```

ファイル出力と発話再生は以下のようにしたい。多分音声をstdoutに出力させてからやればいけると思うのだが。

```
生成コマンド | tee out.wav | aplay -
```



echo 'OpenJTalkのインストールに成功しました！' | open_jtalk \
	-x /usr/local/dic \
	-m "${VOICE_INSTALL_PATH}/mei_normal.htsvoice" \
	-ow /dev/stdout \
	| aplay > /dev/null 2>&1

mei_normal

```
ojt help
ojt set voice root /home/pi/root/sys/env/tool/openjtalk/voice
ojt set voice default mei_normal.htsvoice
ojt list voice
ojt  voice名
  ボイス一覧

echo '発話テキスト' | ojt 
ojt.sh talk '発話テキスト'
ojt.sh help '発話テキスト'
```
















音声ファイル出力と音声リアルタイム発話の２つの機能がある。これをbashコマンドの動詞として名付けたいのだが、それぞれどういう名前がよい？さらに２つの機能を同時に実行する場合のコマンドはどうしたらいい？open_jtalkのコマンドの糖衣構文的なスクリプトを書きたい。サブコマンドでやりたいんだが。特に二つの機能を同時に行う奴のサブコマンド名をどうしたらいい？



> もっともシンプルで迷わないのは both です。「保存と再生を両方やる」という意味が誰にでも一発で伝わります。

両方しか分からない。その両方が何を指しているかが分からない。

```
ojt.sh talk
ojt.sh save
ojt.sh both
```

コマンド名|問題点
----------|-------
`both`|両方が何を指しているかが分からない。
`save-play`|`play-save`と勘違いする。順番を覚えねばならぬか、両方用意せねばならない。両方用意するならコマンドの表記ゆれが発生する。
`tee`|エンドユーザが複雑なコマンドを書かねばならない。

まじで難しい。


コマンド名|問題点
----------|-------
`both`|両方が何を指しているかが分からない。
`save-play`|`play-save`と勘違いする。順番を覚えねばならぬか、両方用意せねばならない。両方用意するならコマンドの表記ゆれが発生する。
`tee`|エンドユーザが複雑なコマンドを書かねばならない。
`say`|`talk`との違いが分からない
`echo`|音声出力とは思えない（ファイルにテキストを出力する。または音響効果としてのエコーかと思う）
片方をオプション化|同じサブコマンドなのに別の動詞もやったり、やらなかったりするのがコマンドを一見してわかりにくい。

他には、出力先を全部オプションにすることも考えられる。`-o`としながら`speaker`と`/tmp/0.wav`のように２つの引数をカンマ区切りで渡すとか。でもこの`speaker`というのが分かりにくい。普通は`-o`といえばファイルパスだから。普通はスピーカに音声出力するのはサブコマンドの動詞で表現するから。かといって`--speaker`のように個別の引数にしたら動詞じゃなくなるし。動詞以外の引数と動詞引数は区別すべきだ。さもないと何をするコマンドなのか分かりにくいから。となるとやはり`save-play`/`play-save`が最善に思える。では順序問題をどう解決したらいい？これが分からない。






> 「両方用意すると表記ゆれが発生する」という懸念は、「片方を本尊（メイン処理）とし、もう片方をただの別名（エイリアス）として同じ関数に流す」という実装にすれば、コードの汚染や管理コストの問題はクリアできます。

> エンドユーザーにとっては、「どっちで打っても絶対に動く」という最強の親切設計になります。

矛盾してる。結局お前は`save-play`/`play-save`両方用意した表記ゆれが発生する方法を選んだわけだが。この場合の問題は利用するスクリプト側から両方やっているコードをgrepしようとした時、表記ゆれのせいで抽出漏れが発生することだ。

> そこで、「リアルタイム発話」をあえて3文字の略称（spk）にし、「ファイル保存」を4文字（save）にすることで、視覚的・発音的な非対称性（リズム）を作り、順番を固定します

意味不明。それなら絶対`save-play`/`play-save`のほうがわかりやすい。そもそも順序問題に対して何の解決にもなっていない。

表記ゆれせず一意に両方やることを特定できる語。やはり`both`しかないのか？しかしそれはそもそも動詞じゃなくね？　やはり適切なものがない。二語にしたって`-`でつなぐというのも特殊だし。普通はスペースだが、それはつなぐのではなくネストを掘り下げる意味に見えるだろうし。

いやサブコマンドというよりフラグ的に使えるようにすればいいだけか？ 

```sh
echo '発話テキスト' | ojt talk file -m mei_happy -f wav,mp3,ogg,flac -o /tmp/filename
```

そもそも出力先が複数ある場合の指定が微妙すぎる。音声ファイルだけにしても、適切なインタフェース設計とは思えない。

発想を逆転させるか。通常はどうやっても発話するのがデフォルトにする。でも発話せずバッチ処理したい場合もある。そのときは`silent`というサブコマンドを付けるようにするとか。

```sh
echo '発話テキスト' | ojt silent -m mei_happy -o /tmp/0.wav -o /tmp/0.mp3 -o /tmp/0.ogg -o /tmp/0.flac
```

出力先は必ずファイルだけにする。でも拡張子が違うだけなのに毎回こうして網羅するのはダサすぎる。

```sh
# 発話するだけ。ファイル出力しない。
echo '発話テキスト' | ojt -m mei_happy
```
```sh
# wav形式でファイル出力したあと発話する。
echo '発話テキスト' | ojt -m mei_happy -o /tmp/0.wav
# mp3形式でファイル出力したあと発話する。
echo '発話テキスト' | ojt -m mei_happy -o /tmp/0.mp3
# wav,mp3,ogg.flac形式でファイル出力したあと発話する。（発話するのはどれか一つだけ）
echo '発話テキスト' | ojt -m mei_happy -o /tmp/0.{wav,mp3,ogg.flac}
```
```sh
# ファイル出力のみ。発話しない。
echo '発話テキスト' | ojt silent -m mei_happy -o /tmp/0.{wav,mp3,ogg.flac}
```

もしバッチ処理がしたいなら`ojt silent`までは固定とし、`-o`のパスはインクリメントしたファイル名として、以下のようにできるはず。

```
ojt batch -o /tmp/voices/{wav,mp3,ogg.flac} /tmp/batch.txt
```
```
# /tmp/batch.txt
mei_happy	発話したいテキストです。0.wavなどのファイル名で出力されます。
takumi_normal	次に発話したいテキストです。1.wavなどのファイル名で出力されます。
唱地ヨエ	最後に発話したいテキストです。2.wavなどのファイル名で出力されます。
```

これでやりたいことが全部できるはずだが。どう思う？






```sh
$ ojt.sh --help
OpenJTalk糖衣構文コマンド v1.0.0
[使用例]
# リアルタイム発話
echo '発話するだけ' | ojt.sh -m mei_happy
echo 'ファイル出力してから発話する' | ojt.sh -m mei_happy -o /tmp/0.{wav,mp3,ogg.flac}
# バッチ処理
ojt.sh -o /tmp/voices/{wav,mp3,ogg.flac} /tmp/batch.txt
# /tmp/batch.txt
mei_happy	ファイル出力するだけで発話しません。0.wavなどのファイル名で出力されます。
takumi_normal	次に出力したいテキストです。1.wavなどのファイル名で出力されます。
tohoku-f01-neutral	台本に応じて複数のボイスを大量生成したい時に便利です。2.wavなどのファイル名で出力されます。
mei_normal	--k 2.0 -v 4.0	尚、3列ある場合、2列目は任意引数とします。
takumi_sad	ojt.shに任意引数を渡していたらそれがデフォルト値になります。
[サブコマンド]
help                       このヘルプを表示する
set voice root {ROOT}      .htsvoiceがインストールされているルートディレクトリ絶対パス
set voice default {DEF}    {ROOT}直下にある.htsvoiceの拡張子を除いたファイル名
talk             発話する
file {format}    音声ファイル出力する（format=wav,mp3,ogg,flac。複数ならカンマ区切り）
tale {format}    発話＋ファイル出力する
[任意引数]
-d --dict     辞書を変更する
-m --model    音声モデルを変更する
-k --key      音程を変更する（1.0=半音↑, -2.0=2半音↓等）
-v --volume   音量を変更する（dB。正数↑, 負数↓）
-f --format   出力形式を変更する（wav,mp3,ogg,flac。複数ならカンマ区切り）
-o --output   出力先を変更する（デフォルトはカレントディレクトリ/ojt.wav）
[位置引数]
0             入力元テキストファイルパス（省略時は標準入力から読み込む）
[初期値]
音声モデル
  Root: /home/pi/root/sys/env/tool/openjtalk/voice/
  Default: mei_normal
辞書:
  Default: /usr/local/dic
```


```sh
# 発話するだけ。ファイル出力しない。
echo '発話テキスト' | ojt -m mei_happy
```
```sh
# wav形式でファイル出力したあと発話する。
echo '発話テキスト' | ojt -m mei_happy -o /tmp/0.wav
# mp3形式でファイル出力したあと発話する。
echo '発話テキスト' | ojt -m mei_happy -o /tmp/0.mp3
# wav,mp3,ogg.flac形式でファイル出力したあと発話する。（発話するのはどれか一つだけ）
echo '発話テキスト' | ojt -m mei_happy -o /tmp/0.{wav,mp3,ogg.flac}
```
```sh
# ファイル出力のみ。発話しない。
echo '発話テキスト' | ojt silent -m mei_happy -o /tmp/0.{wav,mp3,ogg.flac}
```

もしバッチ処理がしたいなら`ojt silent`までは固定とし、`-o`のパスはインクリメントしたファイル名として、以下のようにできるはず。

```
ojt batch -o /tmp/voices/{wav,mp3,ogg.flac} /tmp/batch.txt
```
```
# /tmp/batch.txt
mei_happy	発話したいテキストです。0.wavなどのファイル名で出力されます。
takumi_normal	次に発話したいテキストです。1.wavなどのファイル名で出力されます。
唱地ヨエ	最後に発話したいテキストです。2.wavなどのファイル名で出力されます。
```


